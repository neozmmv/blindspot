# Tun interface

This current wintun.dll file if for AMD64. I'm doing it first, since i guess like 99% of windows users run amd64 instead of arm
but in the future i'll probably add support for arm as well.